;
; ジャンプ命令のテスト
;
; コンソールからフラグに値をセットしてから実行する。
; 停止しない／停止したアドレスでテストできる。
; 停止した後、再度、RUNすると次の命令のテストもできる
; 最低、FLAG[CSZ]を[000],[100],[010],[001],[111]でテスト
;

l0	jc	l1
	halt

l1	jm	l2
	halt

l2	jz	l3
	halt

l3	jmp	l0
	halt