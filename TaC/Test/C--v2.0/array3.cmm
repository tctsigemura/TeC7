/*
 * 配列のアドレス計算
 */
int    b;              // レジスタ変数以外

void main() {
  char[] a;            // バイト配列(レジスタ)
  int   c;             // レジスタ変数
  char  d;

  d = a[0];
  d = a[1];
  d = a[b];
  d = a[c];
}