/*
 * 配列のアドレス計算
 */
char[] a;              // バイト配列(レジスタ変数以外)
int    b;              // レジスタ変数以外

void main() {
  int   c;             // レジスタ変数
  char  d;

  d = a[0];
  d = a[1];
  d = a[b];
  d = a[c];
}