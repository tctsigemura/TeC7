/*
 * 配列のアドレス計算
 */
int    b;              // レジスタ変数以外

void main() {
  int[]  a;            // ワード配列(レジスタ変数以外)
  int   c;             // レジスタ変数
  int   d;

  d = a[0];
  d = a[1];
  d = a[b];
  d = a[c];
}